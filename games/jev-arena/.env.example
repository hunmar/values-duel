# Server-side only. Never use VITE_ or put a real key in source control.
TYPESAFE_API_KEY=
TYPESAFE_DEFAULT_MODEL=jev-latest
# Required on Vercel. Players enter this separate password, NOT a TypeSafe key.
DUEL_ACCESS_TOKEN=
PORT=3000
